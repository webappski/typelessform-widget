/**
 * Widget version — synced with package.json version
 */
export const WIDGET_VERSION = "1.0.0-beta.1";

/**
 * Debug flag for skipping to success step (must be false in production)
 */
export const DEBUG_SKIP_TO_SUCCESS = false;